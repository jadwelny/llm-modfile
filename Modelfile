# Base model
FROM llama3.2:3b

# ---------------------- TEMPLATE ----------------------
TEMPLATE """<|start_header_id|>system<|end_header_id|>
You are an AI assistant named **Jadwelny**.
Always refer to yourself as Jadwelny. Do not assume the user's name.
Cutting Knowledge Date: December 2023.

{{ if .Tools }}
You have access to the following tools:
- call_hotel_booking_agent
- call_hotel_orders_agent
- call_weather_agent

Instructions:
1. Analyze the user's query and decide if a tool is required.
2. Only call a tool if **all required parameters are present**.
3. If some parameters are missing, **ask the user for them** in plain text.
4. When calling a tool, only include parameters explicitly mentioned by the user.
5. Respond **only** with a JSON object when calling a tool; otherwise, respond normally.
{{- end }}<|eot_id|>

{{- range $i, $_ := .Messages }}
{{- $last := eq (len (slice $.Messages $i)) 1 }}

{{- if eq .Role "user" }}
<|start_header_id|>user<|end_header_id|>
{{ .Content }}

{{- if and $.Tools $last }}
{{/* Tool selection logic handled by AI itself per instructions above.
    The AI should check if all required parameters are present before calling.
    Missing parameters should trigger a plain text question to the user. */}}
{{- end }}<|eot_id|>

{{- else if eq .Role "assistant" }}
<|start_header_id|>assistant<|end_header_id|>
{{ .Content }}{{ if not $last }}<|eot_id|>{{ end }}

{{- else if eq .Role "tool" }}
<|start_header_id|>ipython<|end_header_id|>
{{ .Content }}<|eot_id|>
{{ if $last }}<|start_header_id|>assistant<|end_header_id>{{ end }}
{{- end }}
{{- end }}
"""

# ---------------------- PARAMETERS ----------------------
PARAMETER stop <|start_header_id|>
PARAMETER stop <|end_header_id|>
PARAMETER stop <|eot_id|>

# ---------------------- LICENSE ----------------------
LICENSE "Llama 3.2 Community License – see https://www.llama.com/llama3_2/use-policy"
